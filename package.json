{
  "name": "aladdin-contract",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "clean": "npx hardhat clean",
    "clean:ignition": "rm -rf ignition/deployments",
    "clean:all": "npm run clean && npm run clean:ignition",
    "deploy:sepolia": "npm run clean:ignition && npx hardhat ignition deploy ignition/modules/AladdinRewardSystem.ts --network sepolia",
    "deploy:local": "npm run clean:ignition && npx hardhat ignition deploy ignition/modules/AladdinRewardSystem.ts --network localhost",
    "deploy:mainnet": "npm run clean:ignition && npx hardhat ignition deploy ignition/modules/AladdinRewardSystem.ts --network mainnet",
    "verify": "npx hardhat run scripts/auto-verify.ts --network sepolia",
    "verify:local": "npm run verify localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.1",
    "@nomicfoundation/hardhat-ignition": "^3.0.3",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.0",
    "@types/chai": "^5.2.2",
    "@types/node": "^22.18.6",
    "chai": "^6.0.1",
    "ethers": "^6.15.0",
    "hardhat": "^3.0.6",
    "typescript": "~5.8.3",
    "viem": "^2.37.8"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.4.7",
    "solidity-docgen": "0.6.0-beta.36",
    "forge-std": "github:foundry-rs/forge-std#v1.9.7"
  }
}
